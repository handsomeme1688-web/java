spring:
  application:
    name: day6
  datasource:
    url: jdbc:mysql://localhost:3306/slice_v1
    username: root
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver



mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
